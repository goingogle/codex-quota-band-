# Codex额度

本项目让用户在不牺牲小米手环日常健康与通知能力的前提下，在手环上查看 Codex 额度并接收任务状态提醒。

## 文档分工

- `CONTEXT.md`（本文）：产品范围、用户已确认的取舍、隐私红线和暂不支持事项；这是产品语义的主要来源。
- `AGENTS.md`：未来 AI Agent 的执行规则、修改边界和快速命令。
- `docs/current-status.md`：三端当前候选、验收状态、临时版本差异和需要用户决策的冲突；新主任务先读本文。
- `docs/architecture.md`：当前 `0.6.0` 代码的组件、协议、数据流和状态语义。
- `docs/development-guide.md`：构建、测试、真机验收和发布流程入口。
- `docs/build-verification.md`：当前公开候选的构建、测试与产物校验摘要；`docs/device-acceptance.md`：公开的真机验收结论。

如果不同文档出现冲突，先以用户最新明确决策为准；在产品语义不明确时暂停实现并询问，不从旧架构或外部模板推断新需求。

## Language

**Android App**:
日常使用的手机端完整看板，同时承担 Codex 额度同步、任务状态查看、通知接收和手环转发。用户可以直接在手机上快速查看额度和任务状态；它提供比手环更完整的信息，但不替代小米运动健康。
_Avoid_: 只有后台转发能力的桥接、只有通知能力的提醒 App、小米运动健康替代品、日常 AstroBox 桥接

**小米运动健康**:
手环的日常主设备管理应用，负责健康数据同步和普通手机通知。
_Avoid_: 可替换桥接、安装工具

**AstroBox 安装工具**:
仅在首次安装或升级手环 RPK 时临时使用的工具，不参与日常同步与提醒。
_Avoid_: 日常桥接、主设备管理应用

**手环 RPK 首次安装**:
Android 新架构首版仍通过 AstroBox 临时安装一次对应 RPK。Android App 检测手环应用是否已安装；未安装时显示清晰步骤和正确 RPK 文件名，安装完成后再请求小米穿戴通信权限。Android App 首版不承担 RPK 安装器职责。
_Avoid_: 未安装 RPK 时直接显示原始 SDK 错误、把 AstroBox 留在日常链路、首版自行实现 RPK 安装

**首次安装向导**:
Android App 首次打开时逐步检查：小米运动健康已连接小米手环 10、对应 RPK 已安装、Wearable SDK 通信权限已授予、已扫描 Windows 托盘配对二维码、电脑到手机到手环三段链路测试通过。每一步只显示“已完成”或清晰的“需要处理”与修复指引，不暴露原始 SDK 错误；全部完成后进入首页，日常不再显示向导。
_Avoid_: 一次性堆出所有权限请求、让用户手工判断链路故障、完成设置后每次启动重复引导

**权限独立降级**:
用户拒绝某项权限时仍可进入 Android App，并继续使用不依赖该权限的功能。拒绝手环通信权限时保留手机额度、任务看板和手机通知，只把手环功能标为“未授权”；拒绝 Android 系统通知权限时保留应用内看板和手环功能，只停用手机系统通知。设置页提供再次授权入口，不反复弹窗催促。
_Avoid_: 一个权限被拒就阻止整个 App、把未授权误报为设备离线、每次启动重复索取已拒绝权限

**手机独立使用**:
手环不在身边、未连接或 RPK 尚未安装时，用户可以跳过手环步骤，先配对 Windows 并独立使用手机额度与任务看板。首页明确显示“手环未设置”或“手环离线”，手机通知保持可用，手环通知渠道暂时不可用；用户以后可从设置页继续完成手环设置。
_Avoid_: 手环故障阻止手机使用、把未设置显示成链路正常、跳过后找不到继续设置入口

**连接状态区分**:
手机端和手环端明确区分三类状态：“ChatGPT未运行”表示 Windows 仍可连接但官方 ChatGPT Windows 客户端没有运行；“电脑离线”表示 Android 端无法连接已配对的 Windows 电脑；“任务状态不可用”表示电脑链路正常但只读 Hook 无法提供任务事件。三者不得合并为笼统的“连接失败”，并分别提供启动 ChatGPT、检查局域网或修复 Hook 的处理方向。额度无法实时更新时仍显示最近一次可信缓存及同步时间。
_Avoid_: 把 ChatGPT 未启动误报为电脑离线、把 Hook 故障误报为没有任务、只显示无法定位原因的连接失败

**ChatGPT 退出静默**:
官方 ChatGPT Windows 客户端被关闭或退出时，手机和手环只把连接区域静默更新为“ChatGPT未运行”，不发送手机系统通知、不让手环震动，也不把已有任务误标为新的任务终态。重新启动 ChatGPT 后恢复实时状态。
_Avoid_: 用户主动关闭 ChatGPT 时额外提醒、把进程退出当成任务事件、退出后继续显示实时在线

**电脑离线静默**:
电脑关机、断网或离开同一局域网而进入“电脑离线”时，手机和手环只静默更新连接状态并显示最后同步时间，不发送手机系统通知，也不让手环震动。恢复连接后按重连规则同步最新状态。
_Avoid_: 每次关机或切换网络都提醒、把正常离开局域网描述为故障、离线时继续显示实时在线

**缓存保留与过期提示**:
电脑离线后，手机和手环继续保留并显示最后一次可信额度与任务摘要，不自动删除或改成空白。离线后立即使用灰色“缓存”标记并显示最后同步时间；连续超过 24 小时未同步时加强为“数据可能已过期”，但仍允许查看原值，不猜测额度变化。
_Avoid_: 离线后清空仍有参考价值的数据、把缓存冒充实时数据、超过固定时间后擅自推算新额度

**三端版本兼容**:
Android App、手环 RPK 和 Rust Windows 端正式发布时保持统一产品版本号，但运行时依据协议版本而不是仅依据产品版本号判断兼容性。RPK 缺失时阻止继续并引导安装；协议不兼容时停止同步并提示通过 AstroBox 升级正确 RPK；仅产品版本号不同但协议兼容时继续工作，只显示非打扰式升级提示。
_Avoid_: 版本号稍有不同就停用、协议不兼容时静默运行、只显示原始 SDK 错误

**新架构版本号**:
当前统一候选使用 `0.6.0 / versionCode 600`，三端正式包构建和升级验收已完成；只有用户另行明确授权后才可提交、推送或在 GitHub 发布。Windows、APK 和 RPK 的产品版本始终一致，产品仍处于前期验证阶段，暂不升为 `1.0.0`。
_Avoid_: 三端版本号分叉、把 beta 发布给普通用户、验收未通过就上传 GitHub、未完成验收就宣称 1.0

**人工验收与发布授权**:
自动测试和五项高风险技术门槛通过后，仍必须由用户完成 Windows、Android 和小米手环 10 三端真机验收，并明确回复“验收通过”。在收到该明确授权前，代码、预览和安装包只保留在本地，不提交、不推送、不创建或更新 GitHub Release；自动化结果不得代替用户验收。
_Avoid_: 根据测试结果自行推断验收通过、用户未授权就提交或发布、用模拟器代替三端真机体验确认

**APK 与 RPK 发布签名**:
新架构的正式 APK 和 RPK 从第一个正式版本开始使用各自固定的发布签名，以支持后续覆盖升级。签名私钥只保存在用户本地安全位置并制作加密离线备份，不提交 GitHub；开发测试包继续使用独立 debug 签名。GitHub Release 只上传签名后的产物与校验值。
_Avoid_: 用 debug 签名长期公开发布、把私钥提交仓库、不同版本随意更换签名

**早期 Windows 发布信任**:
前期不购买商业 Windows 代码签名证书，允许安装包显示“未知发布者”。GitHub Release 提供 SHA-256 校验值，安装文档解释 SmartScreen 确认步骤；不使用来路不明的免费证书绕过系统。真实用户增加、安装信任成为明显问题后再评估购买。
_Avoid_: 把未知发布者描述成已签名、使用不可信证书、在没有用户需求时提前承担商业证书成本

**本地诊断导出**:
Windows 和 Android 提供一键导出诊断信息，只记录版本、连接阶段、错误代码、重连次数、同步时间和三段链路状态。不得记录任务标题、提示词、回复、命令、文件路径、令牌或配对密钥。日志自动轮换，只保留最近 7 天或限定大小；用户主动操作后才导出，导出前说明内容，不自动上传。
_Avoid_: 无限日志、包含敏感内容、静默上传诊断、把完整执行日志当作支持材料

**额度上游确认凭据**:
为保证额度和可用重置的确认性能，Windows 可以在本机读取 Codex 登录凭据中的访问令牌，并仅向官方额度接口发起低频 HTTPS 请求。令牌只在 Windows 进程内存中用于敏感授权头，随后清零；不得写入日志、缓存、诊断、二维码、局域网载荷、Android 或手环。项目不提供云端中转，手机和手环也永远不能访问该令牌。
_Avoid_: 把令牌同步到其他设备、记录令牌/请求头/响应体、将其用于遥测、广告或任何非额度确认用途

**无遥测**:
首版不加入第三方统计、广告 SDK、用户行为分析或自动崩溃上报。崩溃信息只进入本地裁剪日志，需要反馈时由用户手动导出。除用户主动检查更新和 Windows 为额度确认直连官方接口外，软件不访问公网；未来若需要匿名崩溃上报，必须重新取得明确同意。
_Avoid_: Firebase Analytics、友盟、广告 SDK、静默崩溃上传、把局域网数据用于产品分析

**Windows 完整卸载**:
卸载 Windows 程序时默认删除程序文件、自启动项、局域网发现服务、自身创建的防火墙规则、配对凭据、任务摘要缓存、裁剪日志及本软件添加的 Codex Hook。不得删除 Codex 本身、Codex 对话、其他 Codex 配置或其他 Hook。Android APK 和手环 RPK 由用户分别在对应设备卸载，不尝试远程删除。
_Avoid_: 留下隐蔽自启动或防火墙规则、误删其他 Hook、远程删除手机或手环应用

**手动检查更新**:
Windows 程序和 Android App 默认不在后台访问公网检查更新，日常运行保持纯局域网。只有用户主动点击“检查更新”时才访问 GitHub Releases，并且只读取版本号和下载地址，不上传额度或任务数据。APK、RPK 和 Windows 安装包仍作为同一产品版本发布。
_Avoid_: 后台自动联网检查、把更新请求与任务数据绑定、三端版本分散发布

**Android 原生实现**:
Android App 默认采用原生 Kotlin 和 Jetpack Compose，直接接入已真机验证的小米 Wearable SDK，不使用 Flutter、React Native 或 WebView。技术栈属于工程实现决策；若正式兼容性验证发现 Compose 与目标 SDK 冲突，可在不改变产品语义的前提下改用原生 XML 界面，无需用户重新决策。
_Avoid_: 跨平台桥接层、WebView 应用、把框架选择变成用户负担

**Android 新架构**:
由 Android App 支撑的推荐日常方案，保留小米运动健康的健康同步和普通通知能力。
_Avoid_: 跨平台架构、iPhone 方案

**Android 手机范围**:
面向用户的产品与文档只限定为 Android，不按手机品牌或具体型号拆分，也不使用“兼容性待验证”等额外标签。最低 Android 系统版本由小米 Wearable SDK 要求和构建验证确定。
_Avoid_: 写死某个手机型号、限定小米手机、按品牌维护支持列表

**iPhone 支持暂缓**:
当前新架构从一开始只面向 Android，不在新的 Rust Windows 端、Android App、手环协议、测试或发布流程中引入 iPhone/AstroBox 兼容。未来出现真实需求并有开发精力时再单独立项。
_Avoid_: 在首版预留未经验证的 iPhone 兼容层、让旧协议约束 Android 新架构、对新版本作出 iPhone 支持承诺

**AstroBox 旧架构归档**:
已发布的旧版本继续保留以便历史回溯；现有 AstroBox 插件源码标记为 `legacy`，暂不物理删除，但不再参与新版本构建、测试、README 主流程或 Release 产物。新版本不承诺修复旧架构问题，也不向新用户推荐。
_Avoid_: 删除历史发布、从 Git 历史中清除旧实现、让 legacy 组件继续进入正式构建或支持范围

**GitHub 新旧版引导**:
新架构正式发布后，GitHub README 主体只介绍 Android 新架构、当前安装流程和正式版下载，不同时展开两套安装方式。页面末尾保留简短“旧版归档”入口，说明 AstroBox 旧架构不再维护，并链接历史 Release 供已有用户回溯。
_Avoid_: README 并列展示新旧安装流程、让新用户误装旧版、删除历史 Release

**ChatGPT Windows 任务**:
当前 Windows 电脑上官方 ChatGPT 客户端中能够产生本地官方 Hook 事件的一个代理对话，是任务列表与提醒的唯一记录单位。同一对话中的连续追问只更新原记录的状态和时间，不新增任务；首次请求生成稳定短标题，后续不随意改名。不同对话才显示为不同任务。普通云端聊天、手机 ChatGPT、Remote、API 请求、CLI 任务和其他电脑任务不读取也不同步。
_Avoid_: 把每次追问拆成新任务、读取普通云端聊天、API 请求、CLI 任务、其他电脑任务

**任务状态提醒**:
Android 新架构只显示官方 Hook 能可靠证明的三种状态：`Running` 显示“处理中”，`PermissionRequest` 显示“需要授权”，`Stop` 显示“等待查看”。“等待查看”只表示本轮已经停止执行，不宣称任务已经完成、正在普通提问或已经受阻。`Running` 静默更新；“需要授权”和“等待查看”可按通知设置提醒。
_Avoid_: 把 Stop 猜成已完成、根据回复文字或错误文本猜测等待输入或受阻、把中途工具错误当作任务终态

**状态真实性降级**:
任务状态只基于官方 Hook 或其他已验证稳定事件。当前 Hook 没有独立的完成、普通等待输入或受阻终态，因此首版不显示这些标签；不得读取完整助手回复，或通过错误文本、重试次数和经验规则猜测。官方能力以后提供可靠事件时再扩展。
_Avoid_: 为凑齐状态集合制造推断、把 Stop 自动映射为完成或受阻、隐瞒首版能力缺口

**失焦通知**:
Windows 端只在任务状态变化发生时判断整个官方 ChatGPT Windows 客户端是否为当前前台应用。ChatGPT 任意窗口在前台时，只静默更新任务状态面板；ChatGPT 失焦、最小化或被其他软件取得输入焦点时，才允许发送“需要授权”或“等待查看”提醒。前台期间被抑制的提醒在之后失焦时不补发。判断只依赖官方 Windows 应用包身份，不读取窗口标题或客户端内部页面。
_Avoid_: ChatGPT 前台工作时震动、失焦后补发旧提醒、只按进程名判断、读取窗口标题区分内部页面

**通知模式设置**:
Android App 提供“从不通知”“仅在 ChatGPT 失焦时”“始终通知”三个选项，默认选择“仅在 ChatGPT 失焦时”。该设置控制已启用的手机与手环通知渠道，不影响任务状态面板持续更新。
_Avoid_: 写死单一通知模式、关闭通知时停止状态同步

**通知类型设置**:
Android App 分别提供“等待查看”和“需要授权”两个通知类型开关，默认全部开启。设置界面表现为一个三选一的通知时机选择器加两个通知类型开关；关闭一种提醒不停止对应状态同步。
_Avoid_: 把通知时机做成可同时冲突启用的多个开关、继续展示 Hook 无法证明的完成或受阻开关、关闭提醒时停止状态同步

**通知投递渠道**:
Android App 分别提供默认开启的“手机通知”和“小米手环 10 应用提醒”，并提供默认关闭的“小米手环 8 NFC 通知兼容模式”。手环 10 继续通过 Wearable SDK/RPK 接收摘要；手环 8 NFC 不安装 RPK，而由小米运动健康镜像 Android 系统通知。任务通知时机和通知类型规则同时作用于三个独立渠道；关闭任一或全部渠道都不停止额度与任务看板同步。手环 8 配额摘要只允许用户手动发送，或在首次取得有效数据、向下跨过 75/50/25/10 阈值、重置周期变化时低频自动更新；不得把每次 45 秒同步都转成通知。
_Avoid_: 把三个渠道绑死、默认开启手环 8 兼容模式、高频发送配额状态、关闭通知渠道时停止数据同步、把通知兼容描述成手环快应用

**手机通知强度**:
手机端“等待查看”通知默认静音，仅进入通知栏；“需要授权”默认振动但不播放声音。手环 10 渠道按已验证能力震动一次。手环 8 NFC 兼容模式复用可被小米运动健康读取的系统任务通知，并为配额使用独立低重要度、无振动渠道；最终是否在手环亮屏或震动由 Android、小米运动健康和手环设置共同决定。兼容通知必须留在手机通知栏供系统转发，但不是常驻前台服务，用户可以划掉。Android 系统通知渠道允许用户自行改成有声音、仅振动或完全静音。
_Avoid_: 所有状态默认发声、手机和手环同时高强度提醒、绕过 Android 系统通知偏好、为配额创建不可划掉的常驻通知

**通知打开位置**:
点击单条手机任务通知时打开 Android App 并定位对应任务详情；点击合并通知时打开任务列表并置顶需要授权的任务。手环通知仅在小米 SDK 真机验证支持可靠跳转时打开快应用并定位任务，否则只打开快应用首页。任务详情保持只读，不跳转或控制 ChatGPT Windows 对话。
_Avoid_: 所有通知只打开无关首页、未经验证承诺手环深链、从通知触发 ChatGPT 控制

**提醒合并**:
10 秒内多个 Codex 对话产生的提醒合并为一条，优先级为需要授权、等待查看。手环只震动一次，合并文案显示更新总数和最高优先级摘要；任务面板仍逐条显示真实状态。单个任务显示短标题和状态，同一对话同一状态的重复事件直接去重。
_Avoid_: 多任务同时完成时连续震动、用合并提醒覆盖真实任务列表、重复发送同一状态

**手机前台静默**:
Android App 正在前台显示任务看板时，状态实时更新，但不弹手机系统通知，也不让手环震动；被抑制的提醒之后不补发。手机 App 失焦或锁屏后，再按通知时机和类型设置判断。“需要授权”不设打扰例外。
_Avoid_: 用户正在看手机任务看板时重复提醒、手机失焦后补发旧提醒、为需要授权状态绕过静默规则

**Android 后台策略**:
首版不使用会产生常驻状态栏通知的前台服务，采用普通后台连接和心跳，尽力维持局域网任务看板。引导用户设置应用加锁、允许自启动和无限制电池策略；网络恢复或 App 回到前台时自动重连。手机与手环显示连接状态和最后同步时间。用户接受进程被系统杀死时即时提醒可能丢失，重新连接后只恢复最新任务面板；后续仅在真机频繁丢消息时再评估默认关闭的增强后台模式。
_Avoid_: 常驻状态栏通知、宣称提醒绝对必达、把断连误显示为仍在线

**Android 短时后台验收**:
在 App 已后台锁定、允许自启动、电池策略无限制、手机锁屏且 Android App 不在前台的条件下，用几个短时、可重复的场景触发处理中、等待查看和需要授权状态，手机通知与手环提醒需按设置到达，且不出现常驻状态通知。短时测试不能证明长期必达；如果重复出现丢消息，先优化保活和重连，不静默改用前台服务。项目当前不安排数小时连续测试。
_Avoid_: 把短时测试描述成长期必达、测试失败后偷偷增加常驻通知、忽略锁屏场景

**重连提醒**:
手机断网或后台进程被杀后重新连接时，不补发历史“等待查看”提醒，任务面板直接同步最新状态。若重连时仍存在“需要授权”任务，只合并提醒一次并标明是重连后发现；其他任务只更新面板，不震动。
_Avoid_: 重连后连续补响历史通知、漏掉仍未解决的授权请求、把旧等待查看事件当作新事件

**同步速度目标**:
正常网络且 Android 后台进程仍存活时，Codex 状态变化应在 2 秒内更新手机任务看板，单任务提醒在 3 秒内发出，多任务提醒在 10 秒合并窗口结束后立即发出；打开手环快应用后 5 秒内显示手机保存的最新可信状态；手机与电脑在同一局域网中 15 秒内完成重连。进程已被系统杀死不计入必达承诺。
_Avoid_: 把合并等待误报成延迟故障、对被系统杀死的进程承诺实时、只做平均值不验证上限

**一次配对**:
Android App 与当前 Windows 电脑只在首次使用时通过二维码配对一次，并将配对凭据保存在 Android 系统安全凭据存储中。以后两端进入同一可信局域网后自动重连；仅在重装 App、电脑撤销授权或更换电脑时重新配对。日常“连接电脑”指自动重连，不是重新扫码。
_Avoid_: 每次启动重新扫码、日常输入地址或配对码

**局域网自动发现**:
首次配对后，Android App 先尝试上次成功地址，失败后通过局域网服务发现寻找已配对电脑，并使用配对身份验证目标而不是只信任 IP 或服务名。自动发现仍失败时才提示重新扫码；电脑局域网 IP 变化不应成为日常操作。
_Avoid_: 把 IP 当设备身份、IP 变化就重新配对、要求用户日常手填地址

**无感加密配对**:
Windows 首次启动时生成本机身份密钥；配对二维码携带一次性凭据和电脑公钥指纹，后续局域网连接加密并固定验证已配对电脑身份。Android 使用系统安全存储保存凭据，Windows 只保存必要验证信息并允许托盘撤销。用户端仍只有扫码和确认两个动作，不出现证书警告、密码输入或手动网络配置。
_Avoid_: 明文传输任务标题、只靠局域网 IP 鉴权、自签名证书警告、为加密增加日常操作

**不依赖 Windows 网络分类**:
Windows 端不根据“专用/公共网络”配置自动关闭服务，也不向用户显示“电脑网络受限”之类容易误解的状态。安全由加密连接、设备身份验证和短期配对入口保证；用户负责判断当前网络是否适合使用。
_Avoid_: 网络类型误判导致服务停用、把网络配置责任包装成电脑故障、在公共网络下降级为明文或弱认证

**单手机配对**:
一台 Windows 电脑首版只允许绑定一台当前 Android 手机。配对新手机时明确提示将替换原手机，确认后旧手机凭据立即失效；Windows 托盘提供撤销当前手机入口。不实现多手机状态同步或重复通知协调。
_Avoid_: 默认支持多手机、静默替换旧手机、旧手机在替换后继续读取数据

**单电脑配对**:
一台 Android 手机首版只保留一台当前 Windows 电脑。扫描新电脑的配对二维码时明确提示将替换当前电脑，用户确认后删除旧电脑凭据和对应本地任务摘要，再建立新配对；不合并多台电脑的额度、任务或通知。
_Avoid_: 默认支持多电脑、静默替换当前电脑、把不同电脑的任务混进同一列表

**换手机与重装恢复**:
更换手机或重装 Android App 后不从云端恢复配对凭据、任务摘要或通知设置，用户重新扫描 Windows 端二维码完成配对，设置恢复默认值，旧手机凭据立即失效。手环中已安装且协议兼容的 RPK 无需重复安装，只重新完成通信授权与链路检查。
_Avoid_: 云端备份配对密钥、旧手机更换后继续访问、RPK 已存在时强制重复安装

**短期单次配对码**:
只有用户从 Windows 托盘打开配对窗口时才临时允许新手机配对。二维码成功使用一次后立即失效；未使用时 5 分钟自动过期，可在窗口内重新生成。已配对手机的日常自动重连不受二维码期限影响。
_Avoid_: 长期有效二维码、同一配对码重复绑定、过期后要求重启程序

**Windows 启动方式**:
保留现有托盘菜单中的“登录 Windows 时自动启动”选择按钮及用户当前选择，不新增重复设置，也不强制修改用户的开机启动偏好。
_Avoid_: 强制开机启动、删除现有选择按钮、在手机端重复配置 Windows 启动项

**Windows 首次设置向导**:
Rust Windows 程序首次安装后弹出一次简洁向导，依次检查官方 ChatGPT Windows 客户端、安装只读 Hook、引导用户审核信任、验证额度与任务状态事件、选择是否登录 Windows 时自动启动，并显示手机配对二维码。完成后窗口关闭，日常只保留托盘程序；托盘可重新打开“连接与诊断”。
_Avoid_: 首次安装后只留下无说明托盘图标、每次启动重复向导、把诊断入口藏在命令行

**轻量 Windows 端**:
Android 新架构同时重写轻量原生 Windows 托盘程序并移除 Electron，不继续让 Chromium 常驻。现有安装、额度采集、一次配对、撤销授权、暂停同步、立即刷新、开机启动选择和托盘操作语义保持不变，并增加 ChatGPT Windows 任务监控、焦点判断和手机任务看板同步。验收以程序整个进程树稳定运行时内存不超过 100MB 为准。
_Avoid_: 在 Electron 上继续叠加功能、只看单个子进程内存、以功能增加为由放弃内存目标

**Windows 内存验收口径**:
Rust 程序启动并预热约 10 分钟，在 ChatGPT Windows 客户端、只读 Hook、手机连接和任务同步全部启用时，统计本软件整个进程树的专用工作集，稳定状态不得超过 100MB。二维码或诊断窗口可短暂升高，但关闭后需回落；当前只做短时稳定性采样，不安排数小时连续内存增长测试。
_Avoid_: 只统计单个小进程、用未预热瞬时值验收、忽略长期增长

**Windows CPU 验收**:
Windows 程序在没有任务变化时使用事件驱动和低频心跳，不持续扫描。预热后连续观察 10 分钟，整个进程树平均 CPU 占用低于 1%；单次同步、生成二维码或处理 Hook 事件可短暂升高。程序退出后不得留下后台子进程。
_Avoid_: 毫秒级轮询、只测某个子进程、退出后遗留守护进程

**Windows 原生界面**:
原生 Windows 端不要求像素级复刻现有 Electron 配对窗口和设置界面，允许使用简洁的 Windows 系统原生控件。托盘菜单、二维码配对、撤销设备、暂停/刷新和开机启动等功能及中文语义保持清楚一致。
_Avoid_: 为复刻旧视觉重新引入浏览器或重量级 UI 运行时、以视觉迁移为由改变功能语义

**Electron 原地迁移**:
新 Rust Windows 安装器原地替换当前 Electron 版本：检测并关闭旧程序，保留开机启动选择，先验证新程序可启动再移除旧 Electron 运行文件。旧 AstroBox 配对不迁移，Android 新链路首次重新扫码。安装失败不得破坏旧版；历史安装包和 Release 保留以便手动回退。
_Avoid_: 新旧程序并存自启动、验证前删除旧版、遗留 Chromium 运行文件、宣称旧手机配对已迁移

**Windows 原生技术栈**:
轻量 Windows 端使用 Rust 和 Windows 原生 API 实现，不使用 Tauri、WebView 或其他浏览器壳。优先生成无需额外运行时的原生程序，并在可行处复用现有 Rust AstroBox 插件的协议验证与安全逻辑。
_Avoid_: Tauri、WebView、Electron、为开发速度重新引入浏览器运行时


**任务状态面板**:
手环快应用新增一个可持续查看的任务状态区域，用于显示当前 ChatGPT Windows 任务处于处理中、需要授权或等待查看。`Running` 只静默更新面板，不触发手环提醒；任务状态面板优先占用当前“剩余重置次数”所在区域。
_Avoid_: 正在处理时震动、只能靠瞬时通知查看状态、横向滑动阅读

**任务状态颜色**:
手环任务状态使用固定辅助色：处理中为蓝色、需要授权为黄色、等待查看为绿色、缓存或状态不可用为灰色。颜色只用于状态圆点、短标签或局部标记，正文保持高对比白色，并始终显示状态文字。
_Avoid_: 整块高饱和背景、只靠颜色传达状态、用低对比灰字显示核心信息

**多任务展示**:
任务状态面板最多保留并展示 3 个 ChatGPT Windows 任务，不设置按小时过期。排序优先级为需要授权、正在处理、等待查看；位置不够时，从优先级最低且最旧的任务开始淘汰，同一优先级由新任务覆盖旧任务。每项计划显示短标题、状态和更新时间。
_Avoid_: 只显示一个任务、无限任务列表、按时间删除全部记录、让低关注状态挤掉待处理任务

**手环任务行**:
每条手环任务的短标题最多显示两行，超出后省略，不出现横向滚动。状态与更新时间单独占一行，不与标题挤在一起；三条任务之间使用留白或细分隔线区分，不堆叠拥挤卡片。
_Avoid_: 单行横向滚动标题、无限换行、把标题状态时间塞在同一行

**紧凑相对时间**:
手环任务更新时间统一使用 Codex 风格的紧凑相对时间，例如“1分”“25分”“11小时”“3天”，不混用具体时刻和日期。手机端可显示更完整时间。
_Avoid_: 同时混用刚刚、14:32和7月23日等多套格式、冗长的“分钟前”文案

**手机任务看板**:
Android App 通过局域网展示 Windows Codex 状态，按 Codex 对话去重，显示全部需要授权和正在处理的任务，并另外保留最近 10 条等待查看对话。用户可以手动清空手机上的等待查看记录；不做长期历史档案，也不上传云端。手环端仍只保留优先级最高的 3 条。
_Avoid_: 手机也限制为 3 条、无限保存等待查看任务、云端任务历史

**任务摘要持久化**:
手机和电脑重启后继续保留必要任务摘要。手机保留全部正在处理与需要授权任务，以及最近 10 条等待查看任务；Windows 保留对话 ID、短标题、最后状态和更新时间。数据只保存在本机，并用系统安全存储保护加密密钥；原始提示词与回复不保存。重新连接前统一标记为缓存，手机清空等待查看记录时同步清除 Windows 中对应摘要。
_Avoid_: 重启后丢失全部任务、离线缓存冒充实时状态、落盘保存原始提示词或回复

**从看板隐藏任务**:
手机端允许把长期未处理的任务从手机和手环任务看板中隐藏，但不删除、归档或修改 Codex 对话。若该对话以后产生新的状态变化，应自动重新出现。不按固定天数自动删除未解决任务。
_Avoid_: 把隐藏动作包装成 Codex 控制、自动删除重要未解决任务、任务有新动态后仍永久隐藏

**任务记录清理**:
手机端对正在处理或需要授权任务提供“从看板隐藏”，对等待查看任务提供“删除此记录”，并在等待查看列表顶部提供“清空等待查看”。单条隐藏或删除立即执行；“清空等待查看”需要二次确认。所有操作只修改本地任务摘要，不删除、归档或改变 Codex 对话。
_Avoid_: 清理本地记录时控制 Codex、清空全部时不确认、让等待查看记录只能整批清除

**手机动态首页**:
Android App 首页顶部显示额度同步状态；电脑与手环连接状态使用同一双栏卡片。有需要授权任务时，置顶显示提醒卡片。主额度卡优先显示实际存在的 5 小时额度圆环，并在同一卡片内以横向进度条显示周额度；下方显示任务摘要和紧凑的可用重置次数，设置放在独立设置页。
_Avoid_: 设置项挤占首页、授权请求被额度卡片遮住、正常状态下一打开就是历史任务列表

**手机完整额度**:
Android App 与手环都以 5 小时额度为第一视觉层级，周额度使用横向进度条作为第二层级，可用重置保持紧凑。数据源尚未提供 5 小时窗口时显示灰色 `-- / 暂无数据`；窗口存在但尚未同步时显示 `-- / 待同步`，不得猜测百分比或重置时间。手机和手环继续显示周额度、重置摘要和任务状态。
_Avoid_: 手机端只复制手环简版、虚构缺失额度窗口、把缓存值显示成实时值

**严格只读边界**:
Android App 和手环快应用只查看额度、任务状态、连接状态与提醒，不回复 ChatGPT Windows 任务、不批准或拒绝权限、不回答选项、不新建或停止任务，也不通过 UI 自动化、私有 App Server 或阻塞式 Hook 实现反向控制。需要操作任务时使用官方 ChatGPT Windows 客户端或 ChatGPT Remote。
_Avoid_: 手环审批、手机控制 ChatGPT、自由文本回复、桌面 UI 自动化、把任务看板扩展成第三方 ChatGPT 客户端

**产品名称**:
Windows、Android 和手环继续统一使用“Codex额度”作为产品显示名称，不改为“Codex副屏”。任务状态只是额度查看之外的轻量增强，不将产品宣传为电脑副屏。GitHub 仓库地址保持不变，README 标注非官方开源项目。
_Avoid_: Codex副屏、暗示完整桌面镜像、暗示 OpenAI 或小米官方出品

**Codex 只读 Hook**:
Windows 安装流程为官方 ChatGPT Windows 客户端的本地代理能力配置一个全局只读生命周期 Hook，并由用户首次审核信任，用于把任务生命周期事件发送到本机 Rust 程序。Hook 不读取或回传对话正文，不批准、拒绝、修改或阻塞操作，不连接公网，并可从 Windows 托盘查看状态或卸载。额度采集不依赖该 Hook。
_Avoid_: 解析 Codex 私有数据库推断实时任务状态、Hook 返回控制决策、Hook 访问公网、Hook 失效时连带停止额度同步

**Hook 降级语义**:
Hook 被关闭、未信任、损坏或版本不兼容时，额度继续正常同步；任务区域显示“任务状态不可用”，保留最后一次任务列表并标记为“缓存”和最后更新时间，不发送新的任务提醒。Windows 托盘提供“检查并修复 Hook”入口，但不擅自覆盖用户配置。
_Avoid_: 把 Hook 故障显示为没有任务、继续发送缓存提醒、用修复功能静默覆盖用户 Hook

**任务元数据边界**:
用户明确允许只读 Hook 读取本轮用户请求，在 Windows 本地生成最多 16 个汉字的任务短标题，并通过可信局域网向 Android App 和手环传输短标题、当前状态和最后更新时间。原始请求立即丢弃，不传输、不保存；不传输回复正文、执行过程、项目文件名、完整日志、账号信息或其他任务内容，也不新增云端中转。标题生成需去除换行、网址和疑似密钥。
_Avoid_: 原始提示词落盘、完整请求或回复同步、日志同步、文件信息同步、云端任务中转

**安全活动摘要**:
Android App 首版只显示“正在理解”“正在执行命令”“正在修改文件”“正在使用浏览器”“正在整理结果”等安全活动摘要。摘要由真实 Hook 事件映射，不同步具体命令、参数、文件路径、工具输出或日志，也不保存执行详情。手环统一显示“处理中”。
_Avoid_: 完整命令镜像、参数或路径同步、工具输出同步、猜测进度百分比或剩余时间

**隐藏任务标题**:
默认显示 Windows 本地生成的有意义短标题；Android App 提供“隐藏任务标题”开关，开启后手机和手环只显示“任务 N · 时间”等通用标识，不再下发语义标题。
_Avoid_: 无法关闭语义标题、隐藏标题后仍在手环通知正文泄露标题

**手环纵向浏览**:
手环快应用采用两张 212×520 的原生纵向页面：首页保留周额度与可用重置，第二页显示任务状态。
上下滑动切换页面，不采用横向分页或横向正文；系统边缘侧滑返回必须保持可用。两页结构已随当前候选完成
用户真机验收，后续修改不得把任务列表挤掉额度首屏或拦截系统退出手势。
_Avoid_: 横向正文、横向滚动阅读、为任务状态删除已有额度数据、拦截系统边缘返回

**手环退出手势**:
手环应用在页面顶部、中部和底部等任意滚动位置都必须支持系统屏幕边缘侧滑返回或退出。上下滑只负责纵向浏览，不实现自定义横向滑动，滚动容器不得吞掉系统返回手势。
_Avoid_: 无法侧滑退出、用横向手势切换内容、只能回到页面顶部才能退出

**手环型号范围**:
小米手环 10 已完成 Wearable SDK、RPK 页面和提醒的全部真机验收，是当前正式支持型号。小米手环 8 NFC 提供实验性的通知兼容模式：只在手环原生通知列表查看配额摘要和任务提醒，不安装、不运行手环 RPK，也不承诺自定义页面或直连电脑；完成对应真机验收前不得称为正式支持。其他支持 Xiaomi Vela 和 Wearable SDK 的设备可以实验性尝试，但不承诺布局、通知或通信正常，也不为未经测试的屏幕尺寸提前增加适配代码。
_Avoid_: 把小米手环 9 Pro 的参考实现当作已兼容、把手环 8 通知列表描述成快应用、未经真机验证就宣称正式支持其他型号

**首屏信息架构**:
手环打开后的首屏依次突出系统时间、同步状态和周额度大数字，并在周额度下方显示一行任务摘要，例如“2 个处理中”或“1 个需要授权”。继续向下滑动后显示最多 3 条完整任务列表，再往下显示可用重置次数和到期日。
_Avoid_: 用完整任务列表挤掉首屏周额度、把所有任务状态藏到首屏之外

**手环空任务状态**:
没有任何任务记录时，不保留空白的完整任务列表区域；周额度下方只显示一行“暂无任务”，随后直接上移显示可用重置次数和到期日。系统时间、同步状态与周额度的顶部位置保持稳定，不因任务数量改变而跳动。
_Avoid_: 空列表占据整页、无任务时隐藏任务功能入口、任务数量变化导致顶部额度布局跳动

**双端 UI 预览门槛**:
手环 RPK 与 Android App 在正式修改或实现 UI 前都必须先提供预览并由用户确认。手环预览以两张
212×520 页面、纵向切换与真机可读性为准；手机预览覆盖动态首页、任务列表与设置。预览初稿可交给较轻量的
`gpt-5.6-terra` 子智能体完成，由主智能体按本需求上下文复核，以减少重复生成和额度消耗。
_Avoid_: 未确认预览就修改正式 UI、多个智能体重复生成相同方案、把结构草图宣称为真机视觉验收

**高风险技术门槛**:
完整 UI、安装包和正式产物开发前，先验证五项门槛：只读 Hook 的对话与状态覆盖；Windows ChatGPT 客户端失焦判断；Rust 原型的 100MB 内存与低 CPU；Android 无常驻通知的短时后台存活；手机与 Wearable SDK 手环通知不重复及手环通知跳转能力。短时后台验证不等同于长期必达承诺。任一项失败时继续在本地修改和复测，不发布 GitHub beta 或正式版，也不静默放宽承诺；需要改变产品体验或范围时再由用户决策。
_Avoid_: 跳过风险验证直接全面开发、用结构测试代替真机结论、验收失败仍公开发布、技术门槛失败后静默放宽承诺

**单方案预览**:
预览阶段只制作一套完整推荐方案并集中迭代一轮，不并行生成多套相似视觉方向。手环预览覆盖正常、需要授权和离线缓存；手机预览覆盖正常首页、任务需要授权和设置页。
_Avoid_: 三四套同质方案、只看正常状态、用反复发散替代需求确认
